/**
 * This file was automatically generated by @cosmwasm/ts-codegen@1.10.0.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run the @cosmwasm/ts-codegen generate command to regenerate this file.
 */

import { QueryClient, UseQueryOptions } from '@tanstack/react-query'

import {
  ActiveThresholdResponse,
  Addr,
  ArrayOfString,
  Boolean,
  Config,
  HooksResponse,
  InfoResponse,
  NftClaimsResponse,
  TotalPowerAtHeightResponse,
  VotingPowerAtHeightResponse,
} from '@dao-dao/types/contracts/DaoVotingOnftStaked'
import { getCosmWasmClientForChainId } from '@dao-dao/utils'

import { DaoVotingOnftStakedQueryClient } from '../../../contracts/DaoVotingOnftStaked'
import { contractQueries } from '../contract'
import { indexerQueries } from '../indexer'

export const daoVotingOnftStakedQueryKeys = {
  contract: [
    {
      contract: 'daoVotingOnftStaked',
    },
  ] as const,
  address: (contractAddress: string) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.contract[0],
        address: contractAddress,
      },
    ] as const,
  config: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'config',
        args,
      },
    ] as const,
  nftClaims: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'nft_claims',
        args,
      },
    ] as const,
  hooks: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'hooks',
        args,
      },
    ] as const,
  stakedNfts: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'staked_nfts',
        args,
      },
    ] as const,
  activeThreshold: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'active_threshold',
        args,
      },
    ] as const,
  isActive: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'is_active',
        args,
      },
    ] as const,
  votingPowerAtHeight: (
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'voting_power_at_height',
        args,
      },
    ] as const,
  totalPowerAtHeight: (
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'total_power_at_height',
        args,
      },
    ] as const,
  dao: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'dao',
        args,
      },
    ] as const,
  info: (contractAddress: string, args?: Record<string, unknown>) =>
    [
      {
        ...daoVotingOnftStakedQueryKeys.address(contractAddress)[0],
        method: 'info',
        args,
      },
    ] as const,
}
export const daoVotingOnftStakedQueries = {
  config: <TData = Config>(
    queryClient: QueryClient,
    { chainId, contractAddress, options }: DaoVotingOnftStakedConfigQuery<TData>
  ): UseQueryOptions<Config, Error, TData> => ({
    queryKey: daoVotingOnftStakedQueryKeys.config(contractAddress),
    queryFn: async () => {
      try {
        // Attempt to fetch data from the indexer.
        return await queryClient.fetchQuery(
          indexerQueries.queryContract(queryClient, {
            chainId,
            contractAddress,
            formula: 'daoVotingOnftStaked/config',
          })
        )
      } catch (error) {
        console.error(error)
      }

      // If indexer query fails, fallback to contract query.
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).config()
    },
    ...options,
  }),
  nftClaims: <TData = NftClaimsResponse>(
    queryClient: QueryClient,
    {
      chainId,
      contractAddress,
      args,
      options,
    }: DaoVotingOnftStakedNftClaimsQuery<TData>
  ): UseQueryOptions<NftClaimsResponse, Error, TData> => ({
    queryKey: daoVotingOnftStakedQueryKeys.nftClaims(contractAddress, args),
    queryFn: async () => {
      try {
        // Attempt to fetch data from the indexer.
        return {
          nft_claims: await queryClient.fetchQuery(
            indexerQueries.queryContract(queryClient, {
              chainId,
              contractAddress,
              formula: 'daoVotingOnftStaked/nftClaims',
              args,
            })
          ),
        }
      } catch (error) {
        console.error(error)
      }

      // If indexer query fails, fallback to contract query.
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).nftClaims({
        address: args.address,
      })
    },
    ...options,
  }),
  hooks: <TData = HooksResponse>({
    chainId,
    contractAddress,
    options,
  }: DaoVotingOnftStakedHooksQuery<TData>): UseQueryOptions<
    HooksResponse,
    Error,
    TData
  > => ({
    queryKey: daoVotingOnftStakedQueryKeys.hooks(contractAddress),
    queryFn: async () => {
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).hooks()
    },
    ...options,
  }),
  stakedNfts: <TData = ArrayOfString>(
    queryClient: QueryClient,
    {
      chainId,
      contractAddress,
      args,
      options,
    }: DaoVotingOnftStakedStakedNftsQuery<TData>
  ): UseQueryOptions<ArrayOfString, Error, TData> => ({
    queryKey: daoVotingOnftStakedQueryKeys.stakedNfts(contractAddress, args),
    queryFn: async () => {
      try {
        // Attempt to fetch data from the indexer.
        return await queryClient.fetchQuery(
          indexerQueries.queryContract(queryClient, {
            chainId,
            contractAddress,
            formula: 'daoVotingOnftStaked/stakedNfts',
            args,
          })
        )
      } catch (error) {
        console.error(error)
      }

      // If indexer query fails, fallback to contract query.
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).stakedNfts({
        address: args.address,
        limit: args.limit,
        startAfter: args.startAfter,
      })
    },
    ...options,
  }),
  activeThreshold: <TData = ActiveThresholdResponse>(
    queryClient: QueryClient,
    {
      chainId,
      contractAddress,
      options,
    }: DaoVotingOnftStakedActiveThresholdQuery<TData>
  ): UseQueryOptions<ActiveThresholdResponse, Error, TData> => ({
    queryKey: daoVotingOnftStakedQueryKeys.activeThreshold(contractAddress),
    queryFn: async () => {
      try {
        // Attempt to fetch data from the indexer.
        return await queryClient.fetchQuery(
          indexerQueries.queryContract(queryClient, {
            chainId,
            contractAddress,
            formula: 'daoVotingOnftStaked/activeThreshold',
          })
        )
      } catch (error) {
        console.error(error)
      }

      // If indexer query fails, fallback to contract query.
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).activeThreshold()
    },
    ...options,
  }),
  isActive: <TData = Boolean>({
    chainId,
    contractAddress,
    options,
  }: DaoVotingOnftStakedIsActiveQuery<TData>): UseQueryOptions<
    Boolean,
    Error,
    TData
  > => ({
    queryKey: daoVotingOnftStakedQueryKeys.isActive(contractAddress),
    queryFn: async () => {
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).isActive()
    },
    ...options,
  }),
  votingPowerAtHeight: <TData = VotingPowerAtHeightResponse>(
    queryClient: QueryClient,
    {
      chainId,
      contractAddress,
      args,
      options,
    }: DaoVotingOnftStakedVotingPowerAtHeightQuery<TData>
  ): UseQueryOptions<VotingPowerAtHeightResponse, Error, TData> => ({
    queryKey: daoVotingOnftStakedQueryKeys.votingPowerAtHeight(
      contractAddress,
      args
    ),
    queryFn: async () => {
      try {
        // Attempt to fetch data from the indexer.
        return await queryClient.fetchQuery(
          indexerQueries.queryContract(queryClient, {
            chainId,
            contractAddress,
            formula: 'daoVotingOnftStaked/votingPowerAtHeight',
            args: {
              address: args.address,
            },
            ...(args.height && { block: { height: args.height } }),
          })
        )
      } catch (error) {
        console.error(error)
      }

      // If indexer query fails, fallback to contract query.
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).votingPowerAtHeight({
        address: args.address,
        height: args.height,
      })
    },
    ...options,
  }),
  totalPowerAtHeight: <TData = TotalPowerAtHeightResponse>(
    queryClient: QueryClient,
    {
      chainId,
      contractAddress,
      args,
      options,
    }: DaoVotingOnftStakedTotalPowerAtHeightQuery<TData>
  ): UseQueryOptions<TotalPowerAtHeightResponse, Error, TData> => ({
    queryKey: daoVotingOnftStakedQueryKeys.totalPowerAtHeight(
      contractAddress,
      args
    ),
    queryFn: async () => {
      try {
        // Attempt to fetch data from the indexer.
        return await queryClient.fetchQuery(
          indexerQueries.queryContract(queryClient, {
            chainId,
            contractAddress,
            formula: 'daoVotingOnftStaked/totalPowerAtHeight',
            ...(args.height && { block: { height: args.height } }),
          })
        )
      } catch (error) {
        console.error(error)
      }

      // If indexer query fails, fallback to contract query.
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).totalPowerAtHeight({
        height: args.height,
      })
    },
    ...options,
  }),
  dao: <TData = Addr>(
    queryClient: QueryClient,
    { chainId, contractAddress, options }: DaoVotingOnftStakedDaoQuery<TData>
  ): UseQueryOptions<Addr, Error, TData> => ({
    queryKey: daoVotingOnftStakedQueryKeys.dao(contractAddress),
    queryFn: async () => {
      try {
        // Attempt to fetch data from the indexer.
        return await queryClient.fetchQuery(
          indexerQueries.queryContract(queryClient, {
            chainId,
            contractAddress,
            formula: 'daoVotingOnftStaked/dao',
          })
        )
      } catch (error) {
        console.error(error)
      }

      // If indexer query fails, fallback to contract query.
      return new DaoVotingOnftStakedQueryClient(
        await getCosmWasmClientForChainId(chainId),
        contractAddress
      ).dao()
    },
    ...options,
  }),
  info: contractQueries.info,
}
export interface DaoVotingOnftStakedReactQuery<TResponse, TData = TResponse> {
  chainId: string
  contractAddress: string
  options?: Omit<
    UseQueryOptions<TResponse, Error, TData>,
    'queryKey' | 'queryFn' | 'initialData'
  > & {
    initialData?: undefined
  }
}
export interface DaoVotingOnftStakedInfoQuery<TData>
  extends DaoVotingOnftStakedReactQuery<InfoResponse, TData> {}
export interface DaoVotingOnftStakedDaoQuery<TData>
  extends DaoVotingOnftStakedReactQuery<Addr, TData> {}
export interface DaoVotingOnftStakedTotalPowerAtHeightQuery<TData>
  extends DaoVotingOnftStakedReactQuery<TotalPowerAtHeightResponse, TData> {
  args: {
    height?: number
  }
}
export interface DaoVotingOnftStakedVotingPowerAtHeightQuery<TData>
  extends DaoVotingOnftStakedReactQuery<VotingPowerAtHeightResponse, TData> {
  args: {
    address: string
    height?: number
  }
}
export interface DaoVotingOnftStakedIsActiveQuery<TData>
  extends DaoVotingOnftStakedReactQuery<Boolean, TData> {}
export interface DaoVotingOnftStakedActiveThresholdQuery<TData>
  extends DaoVotingOnftStakedReactQuery<ActiveThresholdResponse, TData> {}
export interface DaoVotingOnftStakedStakedNftsQuery<TData>
  extends DaoVotingOnftStakedReactQuery<ArrayOfString, TData> {
  args: {
    address: string
    limit?: number
    startAfter?: string
  }
}
export interface DaoVotingOnftStakedHooksQuery<TData>
  extends DaoVotingOnftStakedReactQuery<HooksResponse, TData> {}
export interface DaoVotingOnftStakedNftClaimsQuery<TData>
  extends DaoVotingOnftStakedReactQuery<NftClaimsResponse, TData> {
  args: {
    address: string
  }
}
export interface DaoVotingOnftStakedConfigQuery<TData>
  extends DaoVotingOnftStakedReactQuery<Config, TData> {}
