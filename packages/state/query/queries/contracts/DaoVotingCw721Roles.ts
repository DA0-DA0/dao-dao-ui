/**
 * This file was automatically generated by @cosmwasm/ts-codegen@1.10.0.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run the @cosmwasm/ts-codegen generate command to regenerate this file.
 */

import { UseQueryOptions, queryOptions } from '@tanstack/react-query'

import {
  Addr,
  Config,
  InfoResponse,
  TotalPowerAtHeightResponse,
  VotingPowerAtHeightResponse,
} from '@dao-dao/types/contracts/DaoVotingCw721Roles'
import { getCosmWasmClientForChainId } from '@dao-dao/utils'

import { DaoVotingCw721RolesQueryClient } from '../../../contracts/DaoVotingCw721Roles'
import { contractQueries } from '../contract'
import { indexerQueries } from '../indexer'

export const daoVotingCw721RolesQueryKeys = {
  contract: [
    {
      contract: 'daoVotingCw721Roles',
    },
  ] as const,
  address: (chainId: string, contractAddress: string) =>
    [
      {
        ...daoVotingCw721RolesQueryKeys.contract[0],
        chainId,
        address: contractAddress,
      },
    ] as const,
  config: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoVotingCw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'config',
        args,
      },
    ] as const,
  votingPowerAtHeight: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoVotingCw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'voting_power_at_height',
        args,
      },
    ] as const,
  totalPowerAtHeight: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoVotingCw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'total_power_at_height',
        args,
      },
    ] as const,
  dao: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoVotingCw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'dao',
        args,
      },
    ] as const,
  info: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoVotingCw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'info',
        args,
      },
    ] as const,
}
export const daoVotingCw721RolesQueries = {
  config: <TData = Config>({
    chainId,
    contractAddress,
    options,
  }: DaoVotingCw721RolesConfigQuery<TData>) =>
    queryOptions<Config, Error, TData>({
      queryKey: daoVotingCw721RolesQueryKeys.config(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoVotingCw721Roles/config',
              args: {},
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoVotingCw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).config()
      },
      ...options,
    }),
  votingPowerAtHeight: <TData = VotingPowerAtHeightResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoVotingCw721RolesVotingPowerAtHeightQuery<TData>) =>
    queryOptions<VotingPowerAtHeightResponse, Error, TData>({
      queryKey: daoVotingCw721RolesQueryKeys.votingPowerAtHeight(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoVotingCw721Roles/votingPowerAtHeight',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoVotingCw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).votingPowerAtHeight({
          address: args.address,
          height: args.height,
        })
      },
      ...options,
    }),
  totalPowerAtHeight: <TData = TotalPowerAtHeightResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoVotingCw721RolesTotalPowerAtHeightQuery<TData>) =>
    queryOptions<TotalPowerAtHeightResponse, Error, TData>({
      queryKey: daoVotingCw721RolesQueryKeys.totalPowerAtHeight(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoVotingCw721Roles/totalPowerAtHeight',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoVotingCw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).totalPowerAtHeight({
          height: args.height,
        })
      },
      ...options,
    }),
  dao: <TData = Addr>({
    chainId,
    contractAddress,
    options,
  }: DaoVotingCw721RolesDaoQuery<TData>) =>
    queryOptions<Addr, Error, TData>({
      queryKey: daoVotingCw721RolesQueryKeys.dao(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoVotingCw721Roles/dao',
              args: {},
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoVotingCw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).dao()
      },
      ...options,
    }),
  info: contractQueries.info,
}
export interface DaoVotingCw721RolesReactQuery<TResponse, TData = TResponse> {
  chainId: string
  contractAddress: string
  options?: Omit<
    UseQueryOptions<TResponse, Error, TData>,
    'queryKey' | 'queryFn' | 'initialData'
  > & {
    initialData?: undefined
  }
}
export interface DaoVotingCw721RolesInfoQuery<TData>
  extends DaoVotingCw721RolesReactQuery<InfoResponse, TData> {}
export interface DaoVotingCw721RolesDaoQuery<TData>
  extends DaoVotingCw721RolesReactQuery<Addr, TData> {}
export interface DaoVotingCw721RolesTotalPowerAtHeightQuery<TData>
  extends DaoVotingCw721RolesReactQuery<TotalPowerAtHeightResponse, TData> {
  args: {
    height?: number
  }
}
export interface DaoVotingCw721RolesVotingPowerAtHeightQuery<TData>
  extends DaoVotingCw721RolesReactQuery<VotingPowerAtHeightResponse, TData> {
  args: {
    address: string
    height?: number
  }
}
export interface DaoVotingCw721RolesConfigQuery<TData>
  extends DaoVotingCw721RolesReactQuery<Config, TData> {}
