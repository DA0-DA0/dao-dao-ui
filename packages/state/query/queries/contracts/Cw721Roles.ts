/**
 * This file was automatically generated by @cosmwasm/ts-codegen@1.10.0.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run the @cosmwasm/ts-codegen generate command to regenerate this file.
 */

import { UseQueryOptions, queryOptions } from '@tanstack/react-query'

import {
  AllNftInfoResponseForQueryExt,
  ApprovalResponse,
  ApprovalsResponse,
  ContractInfoResponse,
  MinterResponse,
  NftInfoResponseForQueryExt,
  Null,
  NumTokensResponse,
  OperatorResponse,
  OperatorsResponse,
  OwnerOfResponse,
  OwnershipForString,
  QueryExt,
  TokensResponse,
} from '@dao-dao/types/contracts/Cw721Roles'
import { getCosmWasmClientForChainId } from '@dao-dao/utils'

import { Cw721RolesQueryClient } from '../../../contracts/Cw721Roles'
import { indexerQueries } from '../indexer'

export const cw721RolesQueryKeys = {
  contract: [
    {
      contract: 'cw721Roles',
    },
  ] as const,
  address: (chainId: string, contractAddress: string) =>
    [
      {
        ...cw721RolesQueryKeys.contract[0],
        chainId,
        address: contractAddress,
      },
    ] as const,
  ownerOf: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'owner_of',
        args,
      },
    ] as const,
  approval: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'approval',
        args,
      },
    ] as const,
  approvals: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'approvals',
        args,
      },
    ] as const,
  operator: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'operator',
        args,
      },
    ] as const,
  allOperators: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'all_operators',
        args,
      },
    ] as const,
  numTokens: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'num_tokens',
        args,
      },
    ] as const,
  contractInfo: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'contract_info',
        args,
      },
    ] as const,
  nftInfo: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'nft_info',
        args,
      },
    ] as const,
  allNftInfo: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'all_nft_info',
        args,
      },
    ] as const,
  tokens: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'tokens',
        args,
      },
    ] as const,
  allTokens: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'all_tokens',
        args,
      },
    ] as const,
  minter: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'minter',
        args,
      },
    ] as const,
  extension: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'extension',
        args,
      },
    ] as const,
  ownership: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...cw721RolesQueryKeys.address(chainId, contractAddress)[0],
        method: 'ownership',
        args,
      },
    ] as const,
}
export const cw721RolesQueries = {
  ownerOf: <TData = OwnerOfResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesOwnerOfQuery<TData>) =>
    queryOptions<OwnerOfResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.ownerOf(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/ownerOf',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).ownerOf({
          includeExpired: args.includeExpired,
          tokenId: args.tokenId,
        })
      },
      ...options,
    }),
  approval: <TData = ApprovalResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesApprovalQuery<TData>) =>
    queryOptions<ApprovalResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.approval(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/approval',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).approval({
          includeExpired: args.includeExpired,
          spender: args.spender,
          tokenId: args.tokenId,
        })
      },
      ...options,
    }),
  approvals: <TData = ApprovalsResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesApprovalsQuery<TData>) =>
    queryOptions<ApprovalsResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.approvals(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/approvals',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).approvals({
          includeExpired: args.includeExpired,
          tokenId: args.tokenId,
        })
      },
      ...options,
    }),
  operator: <TData = OperatorResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesOperatorQuery<TData>) =>
    queryOptions<OperatorResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.operator(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/operator',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).operator({
          includeExpired: args.includeExpired,
          operator: args.operator,
          owner: args.owner,
        })
      },
      ...options,
    }),
  allOperators: <TData = OperatorsResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesAllOperatorsQuery<TData>) =>
    queryOptions<OperatorsResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.allOperators(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/allOperators',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).allOperators({
          includeExpired: args.includeExpired,
          limit: args.limit,
          owner: args.owner,
          startAfter: args.startAfter,
        })
      },
      ...options,
    }),
  numTokens: <TData = NumTokensResponse>({
    chainId,
    contractAddress,
    options,
  }: Cw721RolesNumTokensQuery<TData>) =>
    queryOptions<NumTokensResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.numTokens(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/numTokens',
              args: {},
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).numTokens()
      },
      ...options,
    }),
  contractInfo: <TData = ContractInfoResponse>({
    chainId,
    contractAddress,
    options,
  }: Cw721RolesContractInfoQuery<TData>) =>
    queryOptions<ContractInfoResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.contractInfo(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/contractInfo',
              args: {},
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).contractInfo()
      },
      ...options,
    }),
  nftInfo: <TData = NftInfoResponseForQueryExt>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesNftInfoQuery<TData>) =>
    queryOptions<NftInfoResponseForQueryExt, Error, TData>({
      queryKey: cw721RolesQueryKeys.nftInfo(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/nftInfo',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).nftInfo({
          tokenId: args.tokenId,
        })
      },
      ...options,
    }),
  allNftInfo: <TData = AllNftInfoResponseForQueryExt>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesAllNftInfoQuery<TData>) =>
    queryOptions<AllNftInfoResponseForQueryExt, Error, TData>({
      queryKey: cw721RolesQueryKeys.allNftInfo(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/allNftInfo',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).allNftInfo({
          includeExpired: args.includeExpired,
          tokenId: args.tokenId,
        })
      },
      ...options,
    }),
  tokens: <TData = TokensResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesTokensQuery<TData>) =>
    queryOptions<TokensResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.tokens(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/tokens',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).tokens({
          limit: args.limit,
          owner: args.owner,
          startAfter: args.startAfter,
        })
      },
      ...options,
    }),
  allTokens: <TData = TokensResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesAllTokensQuery<TData>) =>
    queryOptions<TokensResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.allTokens(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/allTokens',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).allTokens({
          limit: args.limit,
          startAfter: args.startAfter,
        })
      },
      ...options,
    }),
  minter: <TData = MinterResponse>({
    chainId,
    contractAddress,
    options,
  }: Cw721RolesMinterQuery<TData>) =>
    queryOptions<MinterResponse, Error, TData>({
      queryKey: cw721RolesQueryKeys.minter(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/minter',
              args: {},
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).minter()
      },
      ...options,
    }),
  extension: <TData = Null>({
    chainId,
    contractAddress,
    args,
    options,
  }: Cw721RolesExtensionQuery<TData>) =>
    queryOptions<Null, Error, TData>({
      queryKey: cw721RolesQueryKeys.extension(chainId, contractAddress, args),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/extension',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).queryExtension({
          msg: args.msg,
        })
      },
      ...options,
    }),
  ownership: <TData = OwnershipForString>({
    chainId,
    contractAddress,
    options,
  }: Cw721RolesOwnershipQuery<TData>) =>
    queryOptions<OwnershipForString, Error, TData>({
      queryKey: cw721RolesQueryKeys.ownership(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'cw721Roles/ownership',
              args: {},
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new Cw721RolesQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).ownership()
      },
      ...options,
    }),
}
export interface Cw721RolesReactQuery<TResponse, TData = TResponse> {
  chainId: string
  contractAddress: string
  options?: Omit<
    UseQueryOptions<TResponse, Error, TData>,
    'queryKey' | 'queryFn' | 'initialData'
  > & {
    initialData?: undefined
  }
}
export interface Cw721RolesOwnershipQuery<TData>
  extends Cw721RolesReactQuery<OwnershipForString, TData> {}
export interface Cw721RolesExtensionQuery<TData>
  extends Cw721RolesReactQuery<Null, TData> {
  args: {
    msg: QueryExt
  }
}
export interface Cw721RolesMinterQuery<TData>
  extends Cw721RolesReactQuery<MinterResponse, TData> {}
export interface Cw721RolesAllTokensQuery<TData>
  extends Cw721RolesReactQuery<TokensResponse, TData> {
  args: {
    limit?: number
    startAfter?: string
  }
}
export interface Cw721RolesTokensQuery<TData>
  extends Cw721RolesReactQuery<TokensResponse, TData> {
  args: {
    limit?: number
    owner: string
    startAfter?: string
  }
}
export interface Cw721RolesAllNftInfoQuery<TData>
  extends Cw721RolesReactQuery<AllNftInfoResponseForQueryExt, TData> {
  args: {
    includeExpired?: boolean
    tokenId: string
  }
}
export interface Cw721RolesNftInfoQuery<TData>
  extends Cw721RolesReactQuery<NftInfoResponseForQueryExt, TData> {
  args: {
    tokenId: string
  }
}
export interface Cw721RolesContractInfoQuery<TData>
  extends Cw721RolesReactQuery<ContractInfoResponse, TData> {}
export interface Cw721RolesNumTokensQuery<TData>
  extends Cw721RolesReactQuery<NumTokensResponse, TData> {}
export interface Cw721RolesAllOperatorsQuery<TData>
  extends Cw721RolesReactQuery<OperatorsResponse, TData> {
  args: {
    includeExpired?: boolean
    limit?: number
    owner: string
    startAfter?: string
  }
}
export interface Cw721RolesOperatorQuery<TData>
  extends Cw721RolesReactQuery<OperatorResponse, TData> {
  args: {
    includeExpired?: boolean
    operator: string
    owner: string
  }
}
export interface Cw721RolesApprovalsQuery<TData>
  extends Cw721RolesReactQuery<ApprovalsResponse, TData> {
  args: {
    includeExpired?: boolean
    tokenId: string
  }
}
export interface Cw721RolesApprovalQuery<TData>
  extends Cw721RolesReactQuery<ApprovalResponse, TData> {
  args: {
    includeExpired?: boolean
    spender: string
    tokenId: string
  }
}
export interface Cw721RolesOwnerOfQuery<TData>
  extends Cw721RolesReactQuery<OwnerOfResponse, TData> {
  args: {
    includeExpired?: boolean
    tokenId: string
  }
}
