/**
 * This file was automatically generated by @cosmwasm/ts-codegen@1.10.0.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run the @cosmwasm/ts-codegen generate command to regenerate this file.
 */

import { UseQueryOptions, queryOptions } from '@tanstack/react-query'

import {
  Addr,
  Boolean,
  Config,
  DepositInfoResponse,
  HooksResponse,
  QueryExt,
} from '@dao-dao/types/contracts/DaoPreProposeApprovalSingle'
import { getCosmWasmClientForChainId } from '@dao-dao/utils'

import { DaoPreProposeApprovalSingleQueryClient } from '../../../contracts/DaoPreProposeApprovalSingle'
import { contractQueries } from '../contract'
import { indexerQueries } from '../indexer'

export const daoPreProposeApprovalSingleQueryKeys = {
  contract: [
    {
      contract: 'daoPreProposeApprovalSingle',
    },
  ] as const,
  address: (chainId: string, contractAddress: string) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.contract[0],
        chainId,
        address: contractAddress,
      },
    ] as const,
  proposalModule: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'proposal_module',
        args,
      },
    ] as const,
  dao: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'dao',
        args,
      },
    ] as const,
  info: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'info',
        args,
      },
    ] as const,
  config: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'config',
        args,
      },
    ] as const,
  depositInfo: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'deposit_info',
        args,
      },
    ] as const,
  canPropose: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'can_propose',
        args,
      },
    ] as const,
  proposalSubmittedHooks: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'proposal_submitted_hooks',
        args,
      },
    ] as const,
  queryExtension: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApprovalSingleQueryKeys.address(
          chainId,
          contractAddress
        )[0],
        method: 'query_extension',
        args,
      },
    ] as const,
}
export const daoPreProposeApprovalSingleQueries = {
  proposalModule: <TData = Addr>({
    chainId,
    contractAddress,
    options,
  }: SingleChoiceApprovalProposalModuleQuery<TData>) =>
    queryOptions<Addr, Error, TData>({
      queryKey: daoPreProposeApprovalSingleQueryKeys.proposalModule(
        chainId,
        contractAddress
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprovalSingle/proposalModule',
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApprovalSingleQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).proposalModule()
      },
      ...options,
    }),
  dao: <TData = Addr>({
    chainId,
    contractAddress,
    options,
  }: DaoPreProposeApprovalSingleDaoQuery<TData>) =>
    queryOptions<Addr, Error, TData>({
      queryKey: daoPreProposeApprovalSingleQueryKeys.dao(
        chainId,
        contractAddress
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprovalSingle/dao',
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApprovalSingleQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).dao()
      },
      ...options,
    }),
  info: contractQueries.info,
  config: <TData = Config>({
    chainId,
    contractAddress,
    options,
  }: DaoPreProposeApprovalSingleConfigQuery<TData>) =>
    queryOptions<Config, Error, TData>({
      queryKey: daoPreProposeApprovalSingleQueryKeys.config(
        chainId,
        contractAddress
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprovalSingle/config',
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApprovalSingleQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).config()
      },
      ...options,
    }),
  depositInfo: <TData = DepositInfoResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoPreProposeApprovalSingleDepositInfoQuery<TData>) =>
    queryOptions<DepositInfoResponse, Error, TData>({
      queryKey: daoPreProposeApprovalSingleQueryKeys.depositInfo(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprovalSingle/depositInfo',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApprovalSingleQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).depositInfo({
          proposalId: args.proposalId,
        })
      },
      ...options,
    }),
  canPropose: <TData = Boolean>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoPreProposeApprovalSingleCanProposeQuery<TData>) =>
    queryOptions<Boolean, Error, TData>({
      queryKey: daoPreProposeApprovalSingleQueryKeys.canPropose(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async () => {
        return new DaoPreProposeApprovalSingleQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).canPropose({
          address: args.address,
        })
      },
      ...options,
    }),
  proposalSubmittedHooks: <TData = HooksResponse>({
    chainId,
    contractAddress,
    options,
  }: SingleChoiceApprovalProposalSubmittedHooksQuery<TData>) =>
    queryOptions<HooksResponse, Error, TData>({
      queryKey: daoPreProposeApprovalSingleQueryKeys.proposalSubmittedHooks(
        chainId,
        contractAddress
      ),
      queryFn: async () => {
        return new DaoPreProposeApprovalSingleQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).proposalSubmittedHooks()
      },
      ...options,
    }),
  queryExtension: <TData = any>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoPreProposeApprovalSingleQueryExtensionQuery<TData>) =>
    queryOptions<any, Error, TData>({
      queryKey: daoPreProposeApprovalSingleQueryKeys.queryExtension(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async (ctx) => {
        try {
          const query = args.msg
          if ('approver' in query) {
            const approver = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula: 'daoPreProposeApprovalSingle/approver',
              })
            )
            if (approver) {
              return approver
            }
          } else if ('proposal' in query) {
            const proposal = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula: 'daoPreProposeApprovalSingle/proposal',
                args: {
                  id: query.proposal.id,
                },
              })
            )
            if (proposal) {
              return proposal
            }
          } else if ('pending_proposals' in query) {
            const pendingProposals = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula: 'daoPreProposeApprovalSingle/pendingProposals',
                args: {
                  limit: query.pending_proposals.limit,
                  startAfter: query.pending_proposals.start_after,
                },
              })
            )
            if (pendingProposals) {
              return pendingProposals
            }
          } else if ('reverse_pending_proposals' in query) {
            const reversePendingProposals = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula: 'daoPreProposeApprovalSingle/reversePendingProposals',
                args: {
                  limit: query.reverse_pending_proposals.limit,
                  startBefore: query.reverse_pending_proposals.start_before,
                },
              })
            )
            if (reversePendingProposals) {
              return reversePendingProposals
            }
          } else if ('completed_proposals' in query) {
            const completedProposals = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula: 'daoPreProposeApprovalSingle/completedProposals',
                args: {
                  limit: query.completed_proposals.limit,
                  startAfter: query.completed_proposals.start_after,
                },
              })
            )
            if (completedProposals) {
              return completedProposals
            }
          } else if ('reverse_completed_proposals' in query) {
            const reverseCompletedProposals = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula:
                  'daoPreProposeApprovalSingle/reverseCompletedProposals',
                args: {
                  limit: query.reverse_completed_proposals.limit,
                  startBefore: query.reverse_completed_proposals.start_before,
                },
              })
            )
            if (reverseCompletedProposals) {
              return reverseCompletedProposals
            }
          } else if ('completed_proposal_id_for_created_proposal_id' in query) {
            const completedProposalId = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula:
                  'daoPreProposeApprovalSingle/completedProposalIdForCreatedProposalId',
                args: {
                  id: query.completed_proposal_id_for_created_proposal_id.id,
                },
              })
            )
            if (completedProposalId) {
              return completedProposalId
            }
          }
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApprovalSingleQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).queryExtension({
          msg: args.msg,
        })
      },
      ...options,
    }),
}
export interface DaoPreProposeApprovalSingleReactQuery<
  TResponse,
  TData = TResponse,
> {
  chainId: string
  contractAddress: string
  options?: Omit<
    UseQueryOptions<TResponse, Error, TData>,
    'queryKey' | 'queryFn' | 'initialData'
  > & {
    initialData?: undefined
  }
}
export interface DaoPreProposeApprovalSingleQueryExtensionQuery<TData>
  extends DaoPreProposeApprovalSingleReactQuery<any, TData> {
  args: {
    msg: QueryExt
  }
}
export interface SingleChoiceApprovalProposalSubmittedHooksQuery<TData>
  extends DaoPreProposeApprovalSingleReactQuery<HooksResponse, TData> {}
export interface DaoPreProposeApprovalSingleCanProposeQuery<TData>
  extends DaoPreProposeApprovalSingleReactQuery<Boolean, TData> {
  args: {
    address: string
  }
}
export interface DaoPreProposeApprovalSingleDepositInfoQuery<TData>
  extends DaoPreProposeApprovalSingleReactQuery<DepositInfoResponse, TData> {
  args: {
    proposalId: number
  }
}
export interface DaoPreProposeApprovalSingleConfigQuery<TData>
  extends DaoPreProposeApprovalSingleReactQuery<Config, TData> {}
export interface DaoPreProposeApprovalSingleDaoQuery<TData>
  extends DaoPreProposeApprovalSingleReactQuery<Addr, TData> {}
export interface SingleChoiceApprovalProposalModuleQuery<TData>
  extends DaoPreProposeApprovalSingleReactQuery<Addr, TData> {}
