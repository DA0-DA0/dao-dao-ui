/**
 * This file was automatically generated by @cosmwasm/ts-codegen@1.10.0.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run the @cosmwasm/ts-codegen generate command to regenerate this file.
 */

import { UseQueryOptions, queryOptions } from '@tanstack/react-query'

import {
  Addr,
  Boolean,
  Config,
  DepositInfoResponse,
  HooksResponse,
  InfoResponse,
  QueryExt,
} from '@dao-dao/types/contracts/DaoPreProposeApprover'
import { getCosmWasmClientForChainId } from '@dao-dao/utils'

import { DaoPreProposeApproverQueryClient } from '../../../contracts/DaoPreProposeApprover'
import { contractQueries } from '../contract'
import { indexerQueries } from '../indexer'

export const daoPreProposeApproverQueryKeys = {
  contract: [
    {
      contract: 'daoPreProposeApprover',
    },
  ] as const,
  address: (chainId: string, contractAddress: string) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.contract[0],
        chainId,
        address: contractAddress,
      },
    ] as const,
  proposalModule: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'proposal_module',
        args,
      },
    ] as const,
  dao: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'dao',
        args,
      },
    ] as const,
  info: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'info',
        args,
      },
    ] as const,
  config: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'config',
        args,
      },
    ] as const,
  depositInfo: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'deposit_info',
        args,
      },
    ] as const,
  canPropose: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'can_propose',
        args,
      },
    ] as const,
  proposalSubmittedHooks: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'proposal_submitted_hooks',
        args,
      },
    ] as const,
  queryExtension: (
    chainId: string,
    contractAddress: string,
    args?: Record<string, unknown>
  ) =>
    [
      {
        ...daoPreProposeApproverQueryKeys.address(chainId, contractAddress)[0],
        method: 'query_extension',
        args,
      },
    ] as const,
}
export const daoPreProposeApproverQueries = {
  proposalModule: <TData = Addr>({
    chainId,
    contractAddress,
    options,
  }: DaoPreProposeApproverProposalModuleQuery<TData>) =>
    queryOptions<Addr, Error, TData>({
      queryKey: daoPreProposeApproverQueryKeys.proposalModule(
        chainId,
        contractAddress
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprover/proposalModule',
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApproverQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).proposalModule()
      },
      ...options,
    }),
  dao: <TData = Addr>({
    chainId,
    contractAddress,
    options,
  }: DaoPreProposeApproverDaoQuery<TData>) =>
    queryOptions<Addr, Error, TData>({
      queryKey: daoPreProposeApproverQueryKeys.dao(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprover/dao',
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApproverQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).dao()
      },
      ...options,
    }),
  info: contractQueries.info,
  config: <TData = Config>({
    chainId,
    contractAddress,
    options,
  }: DaoPreProposeApproverConfigQuery<TData>) =>
    queryOptions<Config, Error, TData>({
      queryKey: daoPreProposeApproverQueryKeys.config(chainId, contractAddress),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprover/config',
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApproverQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).config()
      },
      ...options,
    }),
  depositInfo: <TData = DepositInfoResponse>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoPreProposeApproverDepositInfoQuery<TData>) =>
    queryOptions<DepositInfoResponse, Error, TData>({
      queryKey: daoPreProposeApproverQueryKeys.depositInfo(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          return await ctx.client.fetchQuery(
            indexerQueries.queryContract({
              chainId,
              contractAddress,
              formula: 'daoPreProposeApprover/depositInfo',
              args,
            })
          )
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApproverQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).depositInfo({
          proposalId: args.proposalId,
        })
      },
      ...options,
    }),
  canPropose: <TData = Boolean>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoPreProposeApproverCanProposeQuery<TData>) =>
    queryOptions<Boolean, Error, TData>({
      queryKey: daoPreProposeApproverQueryKeys.canPropose(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async () => {
        return new DaoPreProposeApproverQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).canPropose({
          address: args.address,
        })
      },
      ...options,
    }),
  proposalSubmittedHooks: <TData = HooksResponse>({
    chainId,
    contractAddress,
    options,
  }: DaoPreProposeApproverProposalSubmittedHooksQuery<TData>) =>
    queryOptions<HooksResponse, Error, TData>({
      queryKey: daoPreProposeApproverQueryKeys.proposalSubmittedHooks(
        chainId,
        contractAddress
      ),
      queryFn: async () => {
        return new DaoPreProposeApproverQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).proposalSubmittedHooks()
      },
      ...options,
    }),
  queryExtension: <TData = any>({
    chainId,
    contractAddress,
    args,
    options,
  }: DaoPreProposeApproverQueryExtensionQuery<TData>) =>
    queryOptions<any, Error, TData>({
      queryKey: daoPreProposeApproverQueryKeys.queryExtension(
        chainId,
        contractAddress,
        args
      ),
      queryFn: async (ctx) => {
        try {
          // Attempt to fetch data from the indexer.
          const query = args.msg
          if ('pre_propose_approval_contract' in query) {
            const preProposeApprovalContract = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula: 'daoPreProposeApprover/preProposeApprovalContract',
              })
            )
            if (preProposeApprovalContract) {
              return preProposeApprovalContract
            }
          } else if (
            'pre_propose_approval_id_for_approver_proposal_id' in query
          ) {
            const proposalId = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula:
                  'daoPreProposeApprover/preProposeApprovalIdForApproverProposalId',
                args: {
                  id: query.pre_propose_approval_id_for_approver_proposal_id.id,
                },
              })
            )
            if (proposalId) {
              return proposalId
            }
          } else if (
            'approver_proposal_id_for_pre_propose_approval_id' in query
          ) {
            const proposalId = await ctx.client.fetchQuery(
              indexerQueries.queryContract({
                chainId,
                contractAddress,
                formula:
                  'daoPreProposeApprover/approverProposalIdForPreProposeApprovalId',
                args: {
                  id: query.approver_proposal_id_for_pre_propose_approval_id.id,
                },
              })
            )
            if (proposalId) {
              return proposalId
            }
          }
        } catch (error) {
          console.error(error)
        }

        // If indexer query fails, fallback to contract query.
        return new DaoPreProposeApproverQueryClient(
          await getCosmWasmClientForChainId(chainId),
          contractAddress
        ).queryExtension({
          msg: args.msg,
        })
      },
      ...options,
    }),
}
export interface DaoPreProposeApproverReactQuery<TResponse, TData = TResponse> {
  chainId: string
  contractAddress: string
  options?: Omit<
    UseQueryOptions<TResponse, Error, TData>,
    'queryKey' | 'queryFn' | 'initialData'
  > & {
    initialData?: undefined
  }
}
export interface DaoPreProposeApproverQueryExtensionQuery<TData>
  extends DaoPreProposeApproverReactQuery<any, TData> {
  args: {
    msg: QueryExt
  }
}
export interface DaoPreProposeApproverProposalSubmittedHooksQuery<TData>
  extends DaoPreProposeApproverReactQuery<HooksResponse, TData> {}
export interface DaoPreProposeApproverCanProposeQuery<TData>
  extends DaoPreProposeApproverReactQuery<Boolean, TData> {
  args: {
    address: string
  }
}
export interface DaoPreProposeApproverDepositInfoQuery<TData>
  extends DaoPreProposeApproverReactQuery<DepositInfoResponse, TData> {
  args: {
    proposalId: number
  }
}
export interface DaoPreProposeApproverConfigQuery<TData>
  extends DaoPreProposeApproverReactQuery<Config, TData> {}
export interface DaoPreProposeApproverInfoQuery<TData>
  extends DaoPreProposeApproverReactQuery<InfoResponse, TData> {}
export interface DaoPreProposeApproverDaoQuery<TData>
  extends DaoPreProposeApproverReactQuery<Addr, TData> {}
export interface DaoPreProposeApproverProposalModuleQuery<TData>
  extends DaoPreProposeApproverReactQuery<Addr, TData> {}
