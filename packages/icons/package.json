{
  "name": "@dao-dao/icons",
  "version": "0.0.0",
  "scripts": {
    "build": "svgr --out-dir dist --typescript --icon --replace-attr-values \"currentColor={props.color || 'currentColor'}\" -- assets/",
    "clean": "rm -rf dist/",
    "dev": "npm run build",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "@dao-dao/config": "*",
    "@svgr/cli": "^6.1.1"
  },
  "prettier": "@dao-dao/config/prettier",
  "private": true
}
